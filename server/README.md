# EcoTrack Backend

## Setup

1. Install dependencies:
   npm install
2. Create environment file:
   copy .env.example .env
3. Start server:
   npm run dev

## Scripts

- npm run dev: Start server with node
- npm run start: Start server with node
